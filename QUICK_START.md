# 快速開始指南 / Quick Start Guide

## 🚀 桌面應用程式（推薦 - 始終置頂！）

### 第一次使用：

1. **安裝依賴**：
   ```bash
   npm install
   ```
   這會自動安裝所有需要的套件，包括 Electron。

2. **啟動桌面應用**（三種方式任選其一）：

   **方式 A：雙擊啟動文件（最簡單！）**
   - 在項目文件夾中找到 `start.bat` 文件
   - 雙擊 `start.bat` 即可啟動
   - 會顯示一個命令窗口，關閉應用程式後可以關閉這個窗口

   **方式 B：使用終端**
   ```bash
   npm run app
   ```

   **方式 C：創建桌面快捷方式**
   - 右鍵點擊 `start.bat` → 選擇「發送到」→「桌面（建立捷徑）」
   - 之後可以直接從桌面雙擊啟動

3. **使用應用**：
   - 視窗會自動置頂在所有其他視窗之上
   - 可以在 Zoom 課程中使用
   - 視窗可以拖動和調整大小
   - 關閉視窗會自動停止伺服器

### 功能說明：

- ✅ **始終置頂**：視窗會保持在所有應用程式之上
- ✅ **自動啟動伺服器**：不需要手動啟動
- ✅ **多視窗支援**：可以同時開啟教師端和學生端視窗
- ✅ **資料自動儲存**：所有資料會自動儲存在 `data/students.txt`

### 使用提示：

- 可以將視窗放在螢幕的一角，不影響其他應用
- 在 Zoom 課程中，視窗會保持在最上層，方便記錄學生答題
- 點擊「Student View」按鈕可以開啟排行榜視窗（也會置頂）

---

## 🌐 瀏覽器模式（備用方案）

如果不想使用桌面應用，也可以使用瀏覽器：

1. **啟動伺服器**：
   ```bash
   npm start
   ```

2. **開啟瀏覽器**，訪問：
   - 教師端：`http://localhost:3000`
   - 學生端：`http://localhost:3000/student.html`

---

## ❓ 常見問題

### Q: 應用程式無法啟動？
A: 確保已經運行 `npm install` 安裝所有依賴。

### Q: 視窗無法置頂？
A: 嘗試重新啟動應用程式。某些系統可能需要管理員權限。

### Q: 端口已被佔用？
A: 關閉其他使用端口 3000 的應用程式，或修改 `server.js` 中的 PORT 設定。

### Q: 想暫時取消置頂？
A: 目前設定為始終置頂。如需修改，可以編輯 `main.js` 中的 `alwaysOnTop` 設定。
